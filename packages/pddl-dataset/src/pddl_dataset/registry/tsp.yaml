name: tsp
binary: ./tsp
domain_file:
  source: static
  path: domain.pddl
output:
  mode: stdout
parameters:
  - name: locations
    type: int
    flag: "-n"
    required: true
    default_test: 3
    description: "Number of locations (minimal 1)"
